server:
  port:
    8099


spring:
  profiles:
    active: docker

---
spring:
  profiles: dev
  data:
    mongodb:
      host: localhost
      database: advert-db

---
spring:
  profiles: docker
  data:
    mongodb:
      host: mongo
      database: advert-db

